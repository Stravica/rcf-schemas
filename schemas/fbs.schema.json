{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.stravica.io/rcf/v0.2.0/fbs.schema.json",
  "title": "RCF Functional Build Spec",
  "description": "Functional Build Specification. One per build session. Carries the mandatory bsId parent reference plus buildOrder and executionStatus. Owns its acIds[] scope (Acceptance Criteria delivered by this FBS) and its dependsOnFbsIds[] graph edge (other FBSs this one depends on).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "fbsId",
    "prdId",
    "bsId",
    "buildOrder",
    "executionStatus",
    "title",
    "summary",
    "acIds",
    "dependsOnFbsIds",
    "createdAt",
    "updatedAt"
  ],
  "properties": {
    "fbsId": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/fbsId" },
    "prdId": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/prdId" },
    "bsId": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/bsId" },
    "buildOrder": { "type": "integer", "minimum": 1 },
    "executionStatus": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/executionStatus" },
    "title": { "type": "string", "minLength": 1 },
    "summary": { "type": "string", "minLength": 1 },
    "approach": { "type": "string" },
    "acIds": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/acId" }
    },
    "dependsOnFbsIds": {
      "type": "array",
      "minItems": 0,
      "items": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/fbsId" }
    },
    "contextRequirements": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "prdSections": {
          "type": "array",
          "items": { "type": "string" }
        },
        "tadSections": {
          "type": "array",
          "items": { "type": "string" }
        },
        "tacIds": {
          "type": "array",
          "items": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/tacId" }
        },
        "adrIds": {
          "type": "array",
          "items": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/adrId" }
        },
        "existingModules": {
          "type": "array",
          "items": { "type": "string" }
        },
        "schemas": {
          "type": "array",
          "items": { "type": "string" }
        },
        "externalDocs": {
          "type": "array",
          "items": { "type": "string" }
        },
        "other": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "estimatedSize": { "enum": ["small", "medium", "large"] },
    "estimatedHours": { "type": "number", "minimum": 0.5, "maximum": 16 },
    "deliverables": {
      "type": "array",
      "items": { "type": "string" }
    },
    "riskLevel": { "enum": ["low", "medium", "high"] },
    "domain": { "type": "string" },
    "notes": { "type": "string" },
    "createdAt": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/timestamp" },
    "updatedAt": { "$ref": "https://schemas.stravica.io/rcf/v0.2.0/common.schema.json#/$defs/timestamp" }
  }
}
